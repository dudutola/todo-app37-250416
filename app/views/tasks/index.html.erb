<h1>My todo List</h1>
<%= link_to "New Task", new_task_path %>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= task.title %>
      <%= task.due_date %>
      <%= task.recurrence %>
    </li>
  <% end %>
</ul>
